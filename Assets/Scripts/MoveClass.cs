using UnityEngine;
using System.Collections;
/*
public class MoveClass : MonoBehaviour
{

    //public void MoveClass.Translate(float x, float y, float z, Space relativeTo = Space.Self);
    public void AddForce(vector3 force);

    void Update()
    {
        // Move the object forward along its z axis 1 unit/second.
        //transform.Translate(Vector3.forward * Time.deltaTime);
        //this.gameObject.rigidbody.AddForce(10, 0, 0, ForceMode.Acceleration);
        //this.GetComponent(Rigidbody).AddForce(10, 0, 0, ForceMode.Acceleration);
        this.GetComponent<Rigidbody>.AddForce(myTransform.forward * 10f, ForceMode.Force);



        // Move the object upward in world space 1 unit/second.
        //transform.Translate(Vector3.up * Time.deltaTime, Space.World);
    }

}
*/